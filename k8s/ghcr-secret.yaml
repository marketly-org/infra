# GHCR image pull secret — lets pods pull images from GitHub Container Registry.
# The deploy script creates this with the actual GitHub token.
---
apiVersion: v1
kind: Secret
metadata:
  name: ghcr-pull-secret
  namespace: marketly
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: "PLACEHOLDER"
